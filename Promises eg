const fs = require("fs");
function benreadfile(){
 return new Promise(function(resolve){
   fs.readFile("a.txt", "utf-8", function(err, data){                   resolve(data);                                                         });
 })
}
function done(data){
  console.log(data);
}

benreadfile().then(done);

